import { NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET() {
  // Placeholder - returns empty array until implemented
  return NextResponse.json([]);
}
